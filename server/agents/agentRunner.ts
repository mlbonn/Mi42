// Agent Runner stub
export async function runAgentJob(job: { id: string; type: string; payload: any }) {
  console.log(`[AgentRunner] Running job ${job.id} of type ${job.type}`);
  // TODO: implement agent job execution
}
